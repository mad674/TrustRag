# Adaptive Retrieval Service

Contains intent classification, BM25 retrieval, hybrid retrieval combining BM25 and dense search, and a reranker (cross-encoder or embedding-based fallback).

Endpoints exposed in `backend/app/api/adaptive.py`.
