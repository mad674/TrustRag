# Backend Services

This folder contains service submodules moved under `backend/services/`:

- `document_ingestion/`
- `document_processing/`
- `embedding_service/`
- `adaptive_retrieval/`
- `langgraph/`
- `evaluation/`

Each service should be implemented as a subpackage inside `backend/services` following the repository pattern.
