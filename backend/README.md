# Backend (FastAPI)

This folder will contain the FastAPI backend. Follow clean architecture and repository pattern.

Next steps:
- scaffold `app/` with routers, services, repositories
- add `requirements.txt` and `Dockerfile`
